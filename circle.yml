machine:
  node:
    version: 0.10.22

test:
  pre:
    - npm install -g karma
    - npm install -g bower
    - bower install

deployment:
  staging:
    branch: dev
    heroku:
      appname: shielded-beyond-2773
